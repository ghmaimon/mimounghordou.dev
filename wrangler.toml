name = "mimounghordou-com"
compatibility_date = "2024-01-01"

# Cloudflare Pages project settings
# Deploy command: (none — static site, no build step)
# Build output directory: /
# Root directory: mimounghordou.dev (set this in Cloudflare Pages dashboard if repo includes other folders)

[env.production]
vars = { ENVIRONMENT = "production" }
